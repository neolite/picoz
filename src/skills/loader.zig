const std = @import("std");

// Placeholder for skills loader
pub const SkillsLoader = struct {
    pub fn init(allocator: std.mem.Allocator) !*SkillsLoader {
        _ = allocator;
        return error.NotImplemented;
    }
};
